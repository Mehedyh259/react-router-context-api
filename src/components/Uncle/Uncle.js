import React from 'react';

const Uncle = ({ house }) => {
    return (
        <div>
            <h2>Uncle</h2>
            <p>Total House: {house}</p>
        </div>
    );
};

export default Uncle;